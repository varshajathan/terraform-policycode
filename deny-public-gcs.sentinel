import "tfplan/v2" as tfplan

main = rule {
  all resources in tfplan.resource_changes as rc {
    rc.type != "google_storage_bucket" or (
      rc.change.after.public_access_prevention is "enforced" and
      rc.change.after.uniform_bucket_level_access
    )
  }
}
